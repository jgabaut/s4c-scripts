[project]
name = "s4c-scripts"
version = "0.2.3"
authors = [
    { name="jgabaut", email="jgabaut@github.com" },
]
description = "An helper for using sprites with ncurses in C."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "GPL-3.0-only"

[project.scripts]
s4c = "s4c.s4c_cli:main"

[project.urls]
Homepage = "https://github.com/jgabaut/s4c-scripts"
Issues = "https://github.com/jgabaut/s4c-scripts/issues"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
